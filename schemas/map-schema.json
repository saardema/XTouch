{
	"title": "Motu XTouch Mapping Configuration",
	"description": "Configuration schema for mapping.toml",
	"type": "object",
	"additionalProperties": false,
	"properties": {
		"mixer": {
			"type": "object",
			"additionalProperties": false,
			"properties": {
				"inputs": {
					"type": "object",
					"description": "Input channel definitions",
					"additionalProperties": {
						"type": "object",
						"description": "Input channel configuration",
						"additionalProperties": false,
						"properties": {
							"fader": {
								"type": "boolean",
								"description": "Makes the Input channel invisible and not configurable",
								"default": true
							}
						}
					}
				},
				"groups": {
					"type": "object",
					"description": "Mix Group definitions",
					"additionalProperties": {
						"type": "object",
						"description": "Mix Group configuration",
						"additionalProperties": false,
						"properties": {
							"send": {
								"type": "boolean",
								"description": "Allow input channels to send to this Group",
								"default": true
							},
							"fader": {
								"type": "boolean",
								"description": "Makes the Group available as a channel strip in the main mix",
								"default": true
							}
						}
					}
				},
				"aux": {
					"type": "object",
					"description": "Auxiliary mix definitions",
					"additionalProperties": {
						"type": "object",
						"description": "Auxiliary mix configuration",
						"additionalProperties": false,
						"properties": {
							"send": {
								"type": "boolean",
								"description": "Allow input channels to send to this Aux mix",
								"default": true
							},
							"fader": {
								"type": "boolean",
								"description": "Allow controlling the final Aux mix output fader",
								"default": true
							}
						}
					}
				}
			}
		},
		"channel_strip": {
			"type": "array",
			"description": "Channel strips are vertical sections of controls consisting of an encoder, 4 buttons and a fader. Defined strips are assigned to controller strips one by one, skipping over disabled definitions.",
			"items": {
				"type": "object",
				"additionalProperties": false,
				"required": [
					"src"
				],
				"properties": {
					"src": {
						"type": "string",
						"description": "Label used to find the corresponding input channel or group by exact match.\n\n Mix input channels are named themselves, otherwise falling back to their input source, which falls back to the input's default name. Similarly, groups are either named or their default."
					},
					"type": {
						"type": "string",
						"enum": [
							"input",
							"group"
						],
						"description": "Type of channel strip source",
						"default": "input"
					},
					"disabled": {
						"type": "boolean",
						"description": "Ignores the strip definition entirely. Useful for temporarily parking an unused mapping",
						"default": false
					}
				}
			}
		}
	}
}